\section{Spectrophotometric Models of Supernovae} \label{sec:spec_phot_model}

Given that the $B^*$ fluxes of supernovae are not directly observable, we rely on spectrophotometric models that allow us to reconstruct the emitted fluxes. Theoretical supernova 
explosion models haven't been able to replicate the observed fluxes with a good precision as seen in \cite{Blinnikov_2006_difficult_theory}, \cite{R_pke_2018_difficult_theory} 
and \cite{Pakmor_2024_difficult_theory}. Instead we rely on empirical models to describe the spectrophotometric data. Here we present the main models that exist. \\ 

\subsection{SALT2/SALT3} \label{sec:SALT2}

The Spectral Adaptive Lightcurve Template (\cite{Guy_2007_SALT2}) class of spectrophotometric models are the current state of the art for cosmological analyses. The idea is to parameterize the model with two parameters that would 
represent the stretch and color in the Tripp relation. These models are particularly useful since they are determined and used on photometric data. 
Spectra are used to determine the spectral features of the model. Once the model has been obtained, one can directly use it on light curves to determine its parameters. \\

We define $\mathcal{S}(\lambda, p)$ as the spectrophotometric evolution of 
Type Ia supernova where $p$ is the phase of the supernova, defined as the difference between the observation date and the date of maximum emission $p = \frac{t_{\text{obs}} - t_{max}}{1+z}$ in days, 
and $\lambda$ the wavelength in $\AA$, both in rest-frame. The goal is to then obtain $S$. \\

We can define the observed spectral flux density as:
\begin{equation}
    \phi_{spec}((1+z)\lambda, (1+z)p) = \frac{1}{4\pi d_L^2(z)} \frac{1}{1+z} \mathcal{S}(\lambda, p) 
    \label{eqn:spectral_flux_density}
\end{equation}

With that we can also deduce the broadband flux observed through a pass-band $T(\lambda)$ as: 

\begin{equation}
    \phi_{phot}((1+z)p) = \frac{1}{4\pi d_L^2(z)} (1+z) \int \mathcal{S}(\lambda, p)\frac{\lambda}{hc}T((1+z)\lambda) \text{d}\lambda
    \label{eqn:broadband_flux}
\end{equation}

As mentioned previously, the luminosity distances of supernovae are defined using the emitted flux in the standard B band at the time of maximum emission, observed at 
$10\text{pc}$ from the supernova: 

\begin{equation}
    \Phi_B = \frac{1}{4\pi(10\text{pc})^2} \int \mathcal{S}(\lambda , p=0) \frac{\lambda}{hc}B(\lambda) \text{d}\lambda
\end{equation}

By imposing the normalization of the model in the $B^*$ band at phase 0:

\begin{equation}
    \int \mathcal{S}(\lambda , p=0) \frac{\lambda}{hc}B(\lambda) \text{d}\lambda = 1
\end{equation}

And defining the following parameter: 

\begin{equation}
    X_0 = \Phi_B \frac{(10\text{pc})^2}{d_L^2(z)}
\end{equation}

We obtain: 

\begin{equation}
    \phi_{spec}((1+z)\lambda, (1+z)p) = X_0 \frac{1}{1+z} \mathcal{S}(\lambda, p)
\end{equation}
\begin{equation}
    \phi_{phot}((1+z)p) = X_0 (1+z) \int \mathcal{S}(\lambda, p)\frac{\lambda}{hc}T((1+z)\lambda) \text{d}\lambda
    \label{eqn:boradband_flux}
\end{equation}

The SALT parameterization of $\mathcal{S}$ is (\cite{Guy_2007_SALT2}):

\begin{equation}
    \boxed{\mathcal{S}(\lambda,p) = [M_0(\lambda,p) + X_1M_1(\lambda, p)]10^{0.4cCL(\lambda)}}
\end{equation}

This parameterization is inspired from a principal component analysis (PCA) with the addition of a color law. The $M_0$ and $M_1$ surfaces along with the $CL$ color law are 
common to all supernovae. $M_0$ is the average spectral surface of a Type Ia supernova (Figure \ref{fig:M0_salt2}), which represents what your average spectrophotometric evolution of a Type Ia 
supernova looks like.
$M_1$ is a surface representing the main variations to $M_0$ (Figure \ref{fig:M1_salt2}). $CL$ represents the color diversity of supernovae (Figure \ref{fig:CL_salt2}). In practice, the measured spectra obtained are only used to 
determine the spectral features of $\mathcal{S}$ as they are not as well calibrated as photometric data and are obtained from many different instruments for the same survey, 
making parameters such as $X_0$ primarily determined from the photometric data. \\

\begin{figure}[ht]
    \centering
    \includegraphics[width=0.8\textwidth]{MainLayout/NaCl_chapter/figures/M0_salt2.pdf}
    \caption{The SALT2.4 $M_0$ surface with respect to the rest-frame wavelength and phase}
    \label{fig:M0_salt2}
\end{figure}

\begin{figure}[ht]
    \centering
    \includegraphics[width=0.8\textwidth]{MainLayout/NaCl_chapter/figures/M1_salt2.pdf}
    \caption{The SALT2.4 $M_1$ surface with respect to the rest-frame wavelength and phase}
    \label{fig:M1_salt2}
\end{figure}

\begin{figure}[ht]
    \centering
    \includegraphics[width=0.8\textwidth]{MainLayout/NaCl_chapter/figures/CL_salt2.pdf}
    \caption{The SALT2.4 color law $CL$ surface with respect to the rest-frame wavelength}
    \label{fig:CL_salt2}
\end{figure}

Determining the global parameters is what we call "training the model" while determining the individual supernova parameters is called "fitting the light curve parameters". 
The model training is performed iteratively on a sample of supernovae, the training dataset. The date of maximum emission of light is the first thing that is determined ($t_{max}$). The 
global surfaces are then fitted and the residuals are analyzed. The variability not captured by the model is then accounted for using an error model. The last two steps are 
then repeated until the model converges. This includes accounting for calibration uncertainties, where for each passband in the dataset requires its own model training in order 
which made the propagation of the calibration uncertainties a difficult task. Once the model has been trained, it is published. It is then fitted on a cosmological dataset, different from the one used in the training, to 
obtain the $X_0$, $X_1$ and $c$ parameters. \\

The initial SALT2 model was trained on a dataset containing 52 low-$z$ supernova from \cite{Hamuy_1996} and 121 high-$z$ supernova from the first two years of observation from SNLS (\cite{Astier_2006}). 
A later retraining of the model was done in \cite{Guy_2010} where 207 new supernovae were added from the SDSS-II sample, giving us the SALT2.4 model. This model was used in the 
\cite{Betoule_2014} JLA cosmological analysis. \\

The SALT3 model is a widely used reimplementation of the SALT model family (\cite{Kenworthy_2021_SALT3}). The entire training procedure was written in Python 3 coding language 
and is publicly available for the community. The model was trained on a 
sample of 1083 supernovae, 2.5 times larger than SALT2. SALT3 improves the training by having consistent photometric handling between surveys and improved cross-calibration between 
surveys in order to reduce survey-dependent systematics. The model was also trained on more high-$z$ supernova adding data from 
PanSTARRS-1, DES and Pantheon, improving the handling of UV data regions. The color law was 
also readjusted to be a higher order polynomial than SALT2. The SALT3 was used for the DES cosmological analysis (\cite{DES_2024}). \\
 

\subsection{Models Trained on the SNfactory Dataset}

The two-parameter standardization scheme introduced by \cite{Tripp_1999} manages to greatly reduce the dispersion between supernovae but still misses on some of the 
variability that is observed. In order to tackle this issue, spectrophotometric models have been developed with the aim of finding standardization parameters based 
on the spectroscopic features of supernovae. To do so, these models have been trained on the SNfactory dataset (\cite{snfactory}). 
A survey that ran from 2004 to 2014 collecting a sequences of spectra of nearby supernovae at low-$z$ (max $z\approx0.11$) over a long period of time. 
SNfactory provides one of the largest homogeneous sets of densely sampled spectrophotometric SN Ia time series. The well calibrated spectra in the SNfactory dataset will also mean 
that it does not need to be accompanied by photometric measurements to acquire distances. \\
These features makes this dataset unique and very interesting to use in order to study the properties of supernova. \\


\subsubsection{SNEMO} 

SNEMO model (SuperNova Empirical MOdels, \cite{Saunders_2018_SNEMO}) generalises the PCA used in SALT2. In SNEMO, the $\mathcal{S}(\lambda, p)$ components are extracted
using an Expectation-Minimization for Factor Analysis or EMFA that takes into account the measurement uncertainties that affect the model. With SNEMO 
multiple model trainings can be done at different orders. The model is expressed as:
\begin{equation}
    S_{SNEMO}(\lambda, p) = c_{SN,0}\left( e_0(\lambda, p) + \sum_{i=1}^{N} c'_{SN,i}e_i(\lambda,p) \right) 10^{0.4 A_s C(\Lambda)}
\end{equation}
where $c_{SN}$ are the individual supernova parameters, $e_i$ the model surfaces, $A_s$ is the supernova extinction and $C$ the extragalactic absorption law introduced by 
\cite{Cardelli_1989} (Cardelli extinction law). This method expands on the traditional SALT parameterization by adding as many extra surfaces as needed to fit the data. 
We can see that at second order the model becomes roughly the same as SALT2 with 2 parameters that represent the color and
stretch of the supernova. Comparisons with the SALT2 color and stretch parameters have shown strong correlations. The \cite{Saunders_2018_SNEMO} paper shows that 
going up to the 7th order the supernovae variability decreases and at the 15th order the model fits the data best. \\ 

\begin{figure}[ht]
    \centering
    \includegraphics[width=0.8\textwidth]{MainLayout/Second_chapter/figures/snemo_flux.png}
    \caption{Model reconstruction of SNEMO on SNfactory spectra (\cite{Saunders_2018_SNEMO}). On the left we see the fluxes of the spectra in comparison with 3 SNEMO models containing 2 (green), 7 (blue) and 15 (red) parameters. The right figures shows the weighted residuals of each model and spectrum.}
    \label{fig:snemo}
\end{figure}


\subsubsection{SUGAR} 

The SUGAR (SUpernova Generator And Reconstructor, \cite{L_get_2020_SUGAR}) model uses EMFA like SNEMO but applied in a different space with the idea that
supernova variability is encoded in their spectral data. Therefore, instead of adding surfaces that would explore the entire space for the standardization features, 
the model has a pre-selected set of spectral features to look out for when determining the model. 
The developers of SUGAR selected 13 spectral features that were shown to capture nearly all of the observed variability of Type Ia supernovae. 
The model for each supernova $i$ is expressed as:
\begin{equation}
    M_i(\lambda, p) = M_0(\lambda, p) + \sum_{j=1}^{3}h_{i, j} \alpha_j(\lambda, p) + A_{V,i} f(\lambda, R_v) + \Delta M_{\text{gray},i}
\end{equation}
with $M_0(\lambda, p)$ being the magnitude of the average supernova spectrum, $\alpha_j(\lambda, p)$ is the intrinsic variation related to the factor $h_{i,j}$, $A_{V,i}$ 
the extinction in the V band, $f(\lambda, R_v)$ is the Cardelli extinction law and $\Delta M_{\text{gray},i}$ is a normalization factor. The parameters $h_{i, 1}$ and 
$h_{i, 3}$ are correlated with the SALT stretch while the $A_{V,i}$ parameter is correlated with the color. 
The work with SUGAR has essentially shown that the supernova variability can be captured by 3 parameters, one more than the traditional stretch and color parameters. \\

\begin{figure}[ht]
    \centering
    \includegraphics[width=0.8\textwidth]{MainLayout/Second_chapter/figures/sugar_salt.png}
    \caption{SUGAR model (blue) vs SALT2.4 (red) on top of the SNfactory data (black) taken from \cite{L_get_2020_SUGAR}.}
    \label{fig:sugar}
\end{figure}


\subsubsection{Twins Embedding} 

The Twins Embedding model (\cite{Boone_2021_twins_embedding}) uses a non-linear parameterization to describe the supernova diversity. It uses a new standardization
method based on the spectral differences between supernovae at specific regions. If the spectral differences between two supernovae are
small enough they are considered as "Twins" (Figure \ref{fig:twins_sn}). This is also called the spectral distance, denoted as $\gamma_{ij}$. The idea is to firstly determine the average spectrum near $t_{max}$, called $f_{mean}$, and account for any redshift uncertainties 
and galactic extinction using the Cardelli extinction law. The variability is then modeled by a non-linear model and adjusted using an algorithm that does a non-linear 
dimensionality reduction called Isomap which is a generalization of PCA while accounting for non-linearities. The spectral distance between two Twins $i$ and $j$ is 
written as:
\begin{equation}
    \gamma_{ij} = \left[ \sum_{k} \frac{f_i(\lambda_k) - f_j(\lambda_k)}{f_{mean}(\lambda_k)} \right]^{\frac{1}{2}}
\end{equation}
with $f_i$ and $f_j$ the spectra of $i$ and $j$. The model is described using three parameters. The first parameter $\xi_1$ highlights the the pseudo-equivalent widths (pEWs) of 
the Ca II features and it describes the most important variabilities. The second parameter $\xi_2$ affects the pEWs of the Si II lines and is correlated with the SALT2 stretch 
parameter $X_1$. The third parameter $\xi_3$ affects the ejecta velocities of the supernova. \\

\begin{figure}[ht]
    \centering
    \includegraphics[width=0.8\textwidth]{MainLayout/Second_chapter/figures/twins_model.png}
    \caption{Two examples of sets of supernova evenly spaced and considered as "Twins". Taken from \cite{Boone_2021_twins_embedding}.}
    \label{fig:twins_sn}
\end{figure}

\begin{figure}[ht]
    \centering
    \includegraphics[width=0.8\textwidth]{MainLayout/Second_chapter/figures/twins_model.png}
    \caption{Variations of the Twins Embedding model near maximum light when varying each one of its components $\xi_1$, $\xi_2$ and $\xi_3$. For each component, the spectrum shown is the median of 20 evenly populated bins of the coordinates of that component. Taken from \cite{Boone_2021_twins_embedding}.}
    \label{fig:twins_model}
\end{figure}


\subsubsection{SuPAErnova} 

The Super Probabilistic Autoencoder model (SuPAErnova) (\cite{Stein_2022_PAE}) uses a probabilistic autoencoder in order to assess the supernova diversity. The use of machine learning (ML) in this case 
leaves the model free to explore different ways to determine the necessary parameterization in order to describe the data. It takes as input 
the spectra from SNfactory and encodes the parameterization of the model. The general definition of the model is:
\begin{equation}
    F_{SN}(\lambda, p) = \mathbf{g}([z_1, ..., z_{n}], p) \times 10^{-0.4[CL(\lambda)\Delta A_V + \Delta M]}
\end{equation}
where $\mathbf{g}$ is a decoder that learns to reconstruct the spectral series, $z_i$ are the latent parameters of the model, $CL$ is the color law, $\Delta A_V$ is the relative 
extrinsic extinction and $\Delta M$ the magnitude residuals. The results shown in the paper indicate that with three latent parameters ($z_1$, $z_2$ and $z_3$) the reconstruction 
errors on the spectral series are minimal. The only drawback is that it then becomes difficult
to have a physical interpretation of the final parameters. \\

\begin{figure}[ht]
    \centering
    \includegraphics[width=0.8\textwidth]{MainLayout/Second_chapter/figures/pae_model.png}
    \caption{Model comparisons from SuPAErnova (red) and SALT2 (blue) on the spectral series of the SN2010dt supernova observed by SNfactory (black) as well as ratios between model and data. Taken from \cite{Stein_2022_PAE}}
    \label{fig:pae_model}
\end{figure}

\section{Main Goal of this PhD}

We present here the overall goal of this PhD: the measurement of $f\sigma_8$ in the local Universe using low redshift supernovae and preparation for a combined analysis using galaxy clustering. 
Two primary avenues in order to conduct such an analysis were explored. The first involved the improvement of how we measure supernova distances. As we have mentioned before the 
measurements of supernova distances play a role in multiple cosmological analyses (dark energy equation of state, $H_0$ and $f\sigma_8$), so one way we can improve the constraining 
power on the growth rate of structures is by improving our distance measurements. The second avenue was regarding the analysis method itself. Current measurements of 
$f\sigma_8$ using Type Ia supernovae have been done using the maximum-likelihood method. This method falls short in reconstructing the nonlinearities in the velocity fields 
since it assumes a linear model. My goal will be to run a field-level forward model inference to extract the information on underlying velocity field while 
accounting for non-linearities. This 
method will also serve as a benchmark for a cross-survey analysis which can be done naturally in this formalism. \\